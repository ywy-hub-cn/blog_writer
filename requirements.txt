# ============================================
# Blog-Writer AI 工作流系统 依赖清单
# ============================================
# 环境配置: conda 环境
# conda create -n blogWriter python=3.12
# conda activate blogWriter
#
# 快速开始:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows)
#   source .venv/bin/activate       (Linux/macOS)
#   pip install -r requirements.txt
#
# 部署方式选择:
#   A) Docker:   docker compose up -d    (一键含 PostgreSQL + Redis + Nginx)
#   B) 原生:     python -m uvicorn blog_writer.main:app --host 0.0.0.0 --port 8000
# ============================================

# ============================================
# 1. 核心框架（必装）
# ============================================
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# ============================================
# 2. HTTP 与 API（必装）
# ============================================
httpx>=0.27.0
python-multipart>=0.0.20
python-dotenv>=1.0.0

# ============================================
# 3. 搜索功能（必装，Agent 工具链依赖）
# ============================================
# 新版包名 ddgs（旧版 duckduckgo-search 已废弃且内部调 Bing 国内不可达）
ddgs>=6.0.0
# 兼容旧导入名，部分代码可能仍 from duckduckgo_search import DDGS
duckduckgo-search>=7.0.0

# ============================================
# 3.1 品牌中文名称转拼音（品牌上传功能依赖，可选；不装自动回退 md5 短码）
# ============================================
pypinyin>=0.50.0

# ============================================
# 4. 可选依赖（按需启用；不装也能跑）
# ============================================

# ---------- 数据库后端 ----------
# 默认 SQLite 无需额外包；启用 PostgreSQL/MySQL 时安装：
# PostgreSQL 驱动（二进制包，包含 libpq）
psycopg2-binary>=2.9.9
# MySQL 驱动
pymysql>=1.1.0

# ---------- 状态外置 ----------
# Redis: Token 外置 / Webhook 持久化；不装自动回退内存存储
redis>=5.0.0

# ============================================
# 5. 开发工具（仅开发测试时需要）
# ============================================
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0

black>=24.10.0
isort>=5.13.0
ruff>=0.7.0
